spring:
  application:
    name: deadlock-java-demo

  datasource:
    url: jdbc:postgresql://localhost:5432/lockdb
    username: postgres
    password: postgres

logging:
  level:
    org.springframework.jdbc.core: DEBUG
